<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easyPoint.dao.administrator.AdminAccountDao">
    <!-- 添加管理员 -->
    <insert id="addAdmin" parameterType="adminAccount">
        INSERT INTO admin_account (phone, username, password, identity, state)
        VALUES (#{phone}, #{username}, #{password}, #{identity}, #{state})
    </insert>

    <!-- 根据手机号码查询账号 -->
    <select id="findAdminByPhone" parameterType="string" resultType="adminAccount">
        SELECT *
        FROM admin_account
        WHERE phone = #{phone}
    </select>

    <!-- 查询所有普通管理员 -->
    <select id="findAllCommonAdmin" resultType="adminAccount">
        SELECT phone, username, identity, state
        FROM admin_account
        WHERE identity = 1
        LIMIT #{startIndex}, #{pageSize}
    </select>

    <!-- 查询所有普通管理员数量 -->
    <select id="findAllCommonAdminNum" resultType="int">
        SELECT COUNT(*)
        FROM admin_account
        WHERE identity = 1
    </select>

    <!-- 禁用或启用普通管理员 -->
    <update id="stopOrStartAdmin">
        UPDATE admin_account
        SET state = #{state}
        WHERE phone = #{phone}
    </update>

    <!-- 删除普通管理员账号 -->
    <delete id="deleteCommonAdmin" parameterType="string">
        DELETE
        FROM admin_account
        WHERE phone = #{phone}
    </delete>

    <!-- 修改密码 -->
    <update id="updatePassword">
        UPDATE admin_account
        SET password = #{newPassword}
        WHERE phone = #{phone}
    </update>

    <!-- 更换手机号码 -->
    <update id="updatePhone">
        UPDATE admin_account
        SET phone    = #{newPhone},
            password = #{newPassword}
        WHERE phone = #{phone}
    </update>
</mapper>