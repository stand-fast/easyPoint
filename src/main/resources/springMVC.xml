<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 开启注解扫描，只扫描Controller注解 -->
    <context:component-scan base-package="com.easyPoint.controller">
        <!--<context:include-filter type="annotation" expression="org.spring-contextngframework.stereotype.Controller"/>-->
    </context:component-scan>

    <context:component-scan base-package="com.easyPoint.controller.miniprogram.travel">
        <!--<context:include-filter type="annotation" expression="org.spring-contextngframework.stereotype.Controller"/>-->
    </context:component-scan>


    <!-- 配置视图解析器对象 -->
    <bean id="internalResourceViewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 过滤静态资源 -->
    <!--<mvc:resources mapping="/js/**" location="/js/"/>-->
    <!--<mvc:resources mapping="/css/**" location="/css/"/>-->
    <!--<mvc:resources mapping="/images/**" location="/images/"/>-->

    <!-- 开启SpringMVC注解的支持 -->
    <mvc:annotation-driven/>


    <mvc:interceptors>
        <mvc:interceptor>
            <!-- 指定拦截的路径 -->
            <mvc:mapping path="/**"/>
            <!-- 指定不拦截的路径 -->
            <mvc:exclude-mapping path="/**/getUserInfoAndToken"/>
            <mvc:exclude-mapping path="/**/pay.do"/>
            <!--//不拦截管理员端-->
            <mvc:exclude-mapping path="/administrator/**"/>
            <bean class="com.easyPoint.interceptor.TokenInterceptor"/>
        </mvc:interceptor>

        <!--<mvc:interceptor>
            &lt;!&ndash; 指定拦截的路径 &ndash;&gt;
            <mvc:mapping path="/administrator/**"/>
            &lt;!&ndash; 指定不拦截的路径 &ndash;&gt;
            &lt;!&ndash; 登录 &ndash;&gt;
            <mvc:exclude-mapping path="/administrator/adminLogin/**"/>
            &lt;!&ndash; 忘记密码 &ndash;&gt;
            <mvc:exclude-mapping path="/administrator/forgetPassword/**"/>
            <bean class="com.easyPoint.interceptor.PcTokenInterceptor"/>
        </mvc:interceptor>-->

    </mvc:interceptors>

    <!-- 文件解析器 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 设置默认编码 -->
        <property name="defaultEncoding" value="utf-8"/>
        <!-- 单个文件最大1M -->
        <property name="maxUploadSizePerFile" value="1048576"/>
        <!-- 总文件最大10M -->
        <property name="maxUploadSize" value="5242880"/>
    </bean>
</beans>